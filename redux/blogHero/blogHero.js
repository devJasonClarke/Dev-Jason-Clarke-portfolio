import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  img: "",
};

export const blogHeroSlice = createSlice({
  name: "blogHero",
  initialState,
  reducers: {
    
  },
});

export default blogHeroSlice.reducer;
